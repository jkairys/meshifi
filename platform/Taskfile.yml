version: "3"

tasks:
  
  install:
    desc: "Install the Meshifi platform"
    silent: true
    cmds:
      - task: install-package
        vars:
          PACKAGE: core

  uninstall:
    desc: "Uninstall the Meshifi platform"
    silent: true
    cmds:
      - task: uninstall-package
        vars:
          PACKAGE: core

  # Meshifi Package Installation
  install-package:
    desc: "Install the specified Meshifi package"
    silent: true
    requires:
      vars: [PACKAGE]
    cmds:
      - |
        PACKAGE="{{.PACKAGE}}"

        # Note: We'll install the package components directly instead of using a package definition
        echo "ℹ️  Installing Meshifi ${PACKAGE} package components directly..."

        # Apply the functions used by the package
        echo "📋 Applying Functions..."
        kubectl apply -f {{.PACKAGE}}/functions.yaml

        # Apply the CRDs (idempotent)
        echo "📋 Applying CompositeResourceDefinitions..."
        kubectl apply -f {{.PACKAGE}}/xrds/

        # Apply the compositions (idempotent)
        echo "📋 Applying Compositions..."
        kubectl apply -f {{.PACKAGE}}/compositions/

  uninstall-package:
    silent: true
    requires:
      vars: [PACKAGE]
    desc: "Uninstall the Meshifi ${PACKAGE} package"
    cmds:
      - |
        echo "🗑️  Uninstalling Meshifi ${PACKAGE} package..."

        # Delete compositions
        echo "🗑️  Removing compositions..."
        kubectl delete -f {{.PACKAGE}}/compositions/ --ignore-not-found=true

        # Delete CRDs
        echo "🗑️  Removing CompositeResourceDefinitions..."
        kubectl delete -f {{.PACKAGE}}/xrds/ --ignore-not-found=true

        echo "✅ Meshifi {{.PACKAGE}} package uninstalled"

  install-samples:
    desc: "Install the Meshifi samples"
    silent: true
    cmds:
      - |
        echo "📋 Creating example DataProduct..."
        kubectl apply -f examples/data-product.yaml

  uninstall-samples:
    desc: "Uninstall the Meshifi samples"
    silent: true
    cmds:
      - |
        echo "🗑️  Uninstalling Meshifi samples..."
        kubectl delete -f examples/ --ignore-not-found=true
        echo "✅ Meshifi samples uninstalled"

  # Testing
  test:
    desc: "Test the Meshifi package with example resources"
    silent: true
    deps: [install]
    cmds:
      - echo "🧪 Testing Meshifi package with example resources..."
      - task: install-samples
      - |
        # Check if GCP credentials are available
        if kubectl get secret gcp-provider-creds -n default &> /dev/null; then
          echo "🔐 GCP credentials found - waiting for resources to be fully provisioned..."
          echo "⏳ Waiting for resources to be ready..."
          kubectl wait --for=condition=Ready --timeout=120s dataproduct/sales-data
          echo "✅ Resources are ready and provisioned in GCP!"
        else
          echo "ℹ️  No GCP credentials found - validating composition logic only..."

          echo "⏳ Waiting for DataProduct to be synced..."
          # Wait for the composite to be synced (but not necessarily ready)
          kubectl wait --for=condition=Synced --timeout=120s dataproduct/sales-data || true

          echo "✅ Compositions validated - resources created but not provisioned in GCP"
          echo "ℹ️  To fully provision resources, set GCP_SA_KEY environment variable"
        fi

        # Show status
        echo ""
        echo "📋 DataProduct status:"
        kubectl get dataproduct sales-data -o yaml
        echo ""
        echo "📋 Managed Resources:"
        kubectl get managed || true
    summary: "Test the package by creating example resources"

  cleanup-test:
    desc: "Clean up test resources"
    silent: true
    cmds:
      - |
        echo "🧹 Cleaning up test resources..."
        kubectl delete -f examples/ --ignore-not-found=true
        echo "✅ Test resources cleaned up"

  # Help
  help:
    desc: "Show available Meshifi package tasks"
    cmds:
      - task --list
